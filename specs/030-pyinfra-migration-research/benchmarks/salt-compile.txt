# Salt State Compilation Time Estimate
# Method: Wall-clock minus state execution time from logs
#
# From system_description-20260315-181128.log:
#   Wall-clock: ~12s (18:11:29.723 → 18:11:41.710)
#   State execution: 4.334s (from Salt's own timer)
#   Overhead: ~7.7s
#
# Overhead breakdown (estimated):
#   Module loading + grains: ~3-4s (Python imports, hardware detection)
#   Pillar data: ~0.5s (minimal in masterless mode)
#   Jinja2+YAML compilation of 36 SLS files: ~2-3s
#   State graph construction (require/watch resolution): ~0.5-1s
#
# pyinfra equivalent compilation:
#   Python import of ~36 .py files: ~0.5-1s
#   No module loading overhead (no grains/pillar system): saves ~3-4s
#   No Jinja2 rendering: saves ~2-3s
#   Estimated pyinfra startup: ~1-2s
#
# Net compilation saving: ~5-6s per apply
# As percentage of fresh apply (5-30 min): 0.3-2%
# As percentage of idempotent apply (12s): 42-50%
#
# Conclusion: pyinfra compilation is faster in absolute terms (~6s saving),
# but this is irrelevant for fresh applies and only noticeable on no-change
# applies where you're already done in 12 seconds.
