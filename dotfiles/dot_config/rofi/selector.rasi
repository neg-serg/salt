/**
 * ROFI Layout
 *
 * HyDE-style Global Selector Style for NixOS
 */

configuration {
    show-icons:                  true;
    drun-display-format:         "{name}";
    font:                        "JetBrainsMono Nerd Font 10";
}

@theme "theme.rasi"

window {
    enabled:                     true;
    fullscreen:                  false;
    transparency:                "real";
    cursor:                      "default";
    spacing:                     0em;
    padding:                     0em;
    border:                      0em;
    border-radius:               0em;
    border-color:                transparent;
    background-color:            @mainbg;
}

mainbox {
    enabled:                     true;
    orientation:                 horizontal;
    children:                    [ "dummy", "frame", "dummy" ];
    background-color:            transparent;
}

frame {
    children:                    [ "listview" ];
    background-color:            transparent;
}

listview {
    enabled:                     true;
    spacing:                     4em;
    padding:                     4em;
    columns:                     6;
    lines:                       1;
    dynamic:                     false;
    fixed-height:                false;
    fixed-columns:               true;
    reverse:                     false;
    cursor:                      "default";
    background-color:            transparent;
    text-color:                  @fgwhite;
}

dummy {
    width:                       2em;
    expand:                      false;
    background-color:            transparent;
}

element {
    enabled:                     true;
    spacing:                     0em;
    padding:                     0em;
    cursor:                      pointer;
    background-color:            transparent;
    text-color:                  @fgwhite;
    orientation:                 vertical;
    border-radius:               15px;
}

element selected.normal {
    background-color:            @selectcolor;
    text-color:                  @fgwhite;
}

element-icon {
    cursor:                      inherit;
    size:                        12em;
    background-color:            transparent;
    text-color:                  inherit;
    expand:                      false;
}

element-text {
    vertical-align:              0.5;
    horizontal-align:            0.5;
    cursor:                      inherit;
    background-color:            transparent;
    text-color:                  inherit;
    enabled:                     true;
}
