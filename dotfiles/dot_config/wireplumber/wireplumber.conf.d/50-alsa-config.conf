monitor.alsa.rules = [
  {
    matches = [
      {
        alsa.card_name = "ADI-2/4 Pro SE (53011083)"
      }
    ]
    actions = {
      update-props = {
        # Profile is managed dynamically or by WirePlumber state.
        # Uncomment to force pro-audio mode if preferred.
        # device.profile = "pro-audio"
      }
    }
  },
  {
    matches = [
      {
        node.name = "~alsa_output.usb-RME_ADI-2_4_Pro_SE.*"
      }
    ]
    actions = {
      update-props = {
        priority.session = 1600
        priority.driver = 1600
      }
    }
  },
  {
    matches = [
      {
        device.name = "~alsa_card.*"
      }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
      }
    }
  }
]
