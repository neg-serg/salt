#!/usr/bin/env zsh
# sx: image viewer wrapper (launches swayimg via swayimg-first session manager)
if ! command -v swayimg > /dev/null 2>&1; then
  print -u2 "sx: swayimg not found (expected at ~/.local/bin/swayimg)"
  exit 1
fi
exec swayimg-first "$@"
