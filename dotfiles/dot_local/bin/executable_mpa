#!/usr/bin/env zsh
setopt ERR_EXIT NOUNSET PIPE_FAIL

# mpa: mpv audio-only (muted) with logging.
# Usage:
#   mpa [mpv-args...] file

log_dir="${HOME}/tmp"
mkdir -p "${log_dir}"

exec mpv -mute "$@" >"${log_dir}/mpv.log" 2>&1

