#!/bin/sh
set -eu
# Restart Pyprland and Quickshell, reload Hyprland config
hyprctl reload > /dev/null 2>&1 || true
sleep 0.15
systemctl --user restart quickshell.service > /dev/null 2>&1 || true
systemctl --user restart pypr.service > /dev/null 2>&1 || true
