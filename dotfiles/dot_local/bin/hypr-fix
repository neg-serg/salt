#!/bin/sh
set -eu
# Restart Pyprland and reload Hyprland config
pkill -x pypr || true
hyprctl reload > /dev/null 2>&1 || true
sleep 0.15
systemctl --user start quickshell.service > /dev/null 2>&1 || true
nohup pypr > /dev/null 2>&1 &
